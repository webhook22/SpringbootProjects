spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: root
        url: jdbc:mysql://localhost:3306/crudoperations?allowPublicKeyRetrieval=true&useSSL=false
        username: root
    jpa:
        hibernate:
            ddl-auto: create-drop